# Format an exact Delta Variant value

Format an exact Delta Variant value

## Usage

``` r
# S3 method for class 'fabric_delta_variant'
format(x, ...)
```

## Arguments

- x:

  A `fabric_delta_variant` object.

- ...:

  Unused.

## Value

A readable character representation.
