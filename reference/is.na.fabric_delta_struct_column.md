# Test parent nullness for a Delta struct column

Test parent nullness for a Delta struct column

## Usage

``` r
# S3 method for class 'fabric_delta_struct_column'
is.na(x)
```

## Arguments

- x:

  A nested Delta struct column.

## Value

A logical vector that is `TRUE` for null parent structs.
