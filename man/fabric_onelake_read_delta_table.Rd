% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_onelake_read_delta_table.R
\name{fabric_onelake_read_delta_table}
\alias{fabric_onelake_read_delta_table}
\title{Read a Delta table from Microsoft Fabric OneLake}
\usage{
fabric_onelake_read_delta_table(
  table_path,
  workspace_name,
  lakehouse_name,
  schema = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  version = NULL,
  dest_dir = NULL,
  verbose = TRUE,
  dfs_base = "https://onelake.dfs.fabric.microsoft.com",
  columns = NULL,
  limit = NULL,
  engine = c("delta-rs", "R"),
  result = c("tibble", "arrow_stream")
)
}
\arguments{
\item{table_path}{Table name, for example \code{"PatientInfo"}. A nested string
is accepted for backward compatibility, but only its final segment is
used; select a schema with \code{schema}.}

\item{workspace_name}{Fabric workspace display name or GUID, or a record
from \code{\link[=fabric_workspaces]{fabric_workspaces()}}.}

\item{lakehouse_name}{Lakehouse or Warehouse item name, GUID, or discovery
record. The argument name is retained for backward compatibility.}

\item{schema}{Lakehouse schema name, for example \code{"dbo"}, or \code{NULL}.
A discovered schema-enabled Lakehouse supplies its default schema
automatically.}

\item{tenant_id}{Microsoft Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}.}

\item{client_id}{Microsoft Entra application/client ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID.}

\item{token}{Optional \code{AzureAuth::AzureToken}, bearer-token string, or
token-provider function.}

\item{auth_args}{Named list passed to \code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}} when no
token source is supplied.}

\item{version}{Optional non-negative Delta transaction version. \code{NULL} reads
the latest snapshot. Versions through \code{2^53} are represented exactly.}

\item{dest_dir}{Local work directory or \code{NULL}. The default creates a
temporary directory and removes it on exit. With \code{engine = "R"}, a supplied
directory retains the staged transaction log and active files. With
\code{engine = "delta-rs"}, it retains the Arrow IPC result. A supplied
directory must be new or empty.}

\item{verbose}{Logical. Show progress.}

\item{dfs_base}{OneLake DFS endpoint.}

\item{columns}{Optional character vector of logical Delta column names to
return, in the requested order.}

\item{limit}{Optional non-negative whole number limiting returned rows.}

\item{engine}{Reader implementation. \code{"delta-rs"} is the default native
engine; \code{"R"} selects the original staged R and DuckDB engine.}

\item{result}{Return format. \code{"tibble"} returns a tibble.
\code{"arrow_stream"} returns a \code{nanoarrow_array_stream} compatible with
\code{\link[arrow:as_record_batch_reader]{arrow::as_record_batch_reader()}} and other Arrow C stream consumers.}
}
\value{
With \code{result = "tibble"}, a tibble containing the selected Delta
snapshot. With \code{result = "arrow_stream"}, a
\code{nanoarrow_array_stream}. Empty tables keep their column schema in either
format.
}
\description{
Reads a Delta table from a Fabric Lakehouse or Warehouse export. By default,
the result is returned as a tibble, so it works like a regular R data frame.
}
\details{
\strong{Choosing an engine}
\itemize{
\item \code{"delta-rs"} is the default and is the best choice for most tables. It
reads OneLake directly with the native
\href{https://github.com/delta-io/delta-rs}{\code{delta-rs}} library.
\item \code{"R"} selects the package's original R and DuckDB reader. Use it for V2
checkpoints, type widening, shredded Variant data, or when you want to
keep a local copy of the table files in \code{dest_dir}.
}

If the selected engine cannot read a table feature, the function returns a
clear error. It never silently switches engines.

\strong{Choosing a result}

\code{result = "tibble"} is simplest for normal R analysis. Use
\code{result = "arrow_stream"} to return a \code{nanoarrow_array_stream}, which can be
passed to \code{\link[arrow:as_record_batch_reader]{arrow::as_record_batch_reader()}} or another Arrow C stream
consumer without first collecting the table as a tibble.

\strong{Technical notes}

Both engines use the same OneLake target resolution, authentication, input
validation, and return formats. The native engine pushes \code{columns} and
\code{limit} into DataFusion and exchanges data with R through Arrow IPC. The R
engine stages the Delta log and active Parquet files, then reads them with
DuckDB.

Delta \code{long} values are returned as \code{bit64::integer64}. Delta decimals are
returned as character vectors so all digits remain exact. Other conversions
follow the selected engine's Arrow or DuckDB conversion.

Tokens use the \verb{https://storage.azure.com/.default} audience. Give the
signed-in user or application Read access through a workspace role or
\strong{Lakehouse > Manage OneLake data access}.
}
\examples{
\dontrun{
df <- fabric_onelake_read_delta_table(
  table_path = "PatientInfo",
  workspace_name = "PatientsWorkspace",
  lakehouse_name = "Lakehouse.Lakehouse"
)

# Use the original staged implementation explicitly.
staged <- fabric_onelake_read_delta_table(
  table_path = "PatientInfo",
  workspace_name = "PatientsWorkspace",
  lakehouse_name = "Lakehouse.Lakehouse",
  engine = "R"
)

# Keep the result in an Arrow-compatible stream.
stream <- fabric_onelake_read_delta_table(
  table_path = "PatientInfo",
  workspace_name = "PatientsWorkspace",
  lakehouse_name = "Lakehouse.Lakehouse",
  result = "arrow_stream"
)
reader <- arrow::as_record_batch_reader(stream)
}
}
\references{
\href{https://github.com/delta-io/delta/blob/master/PROTOCOL.md}{Delta Lake protocol}

\href{https://github.com/delta-io/delta-rs}{delta-rs}

\href{https://learn.microsoft.com/en-us/fabric/onelake/onelake-access-api}{Connect to OneLake}
}
