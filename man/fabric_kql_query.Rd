% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_kql_query.R
\name{fabric_kql_query}
\alias{fabric_kql_query}
\title{Query a Microsoft Fabric Eventhouse with KQL}
\usage{
fabric_kql_query(
  cluster,
  query,
  database = NULL,
  parameters = list(),
  request_properties = list(),
  timeout = 60,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  access_token = NULL,
  token_provider = NULL
)
}
\arguments{
\item{cluster}{Character query-service/cluster URI, or one Eventhouse or
KQLDatabase record returned by \code{\link[=fabric_eventhouses]{fabric_eventhouses()}},
\code{\link[=fabric_kql_databases]{fabric_kql_databases()}}, or \code{\link[=fabric_item]{fabric_item()}}. A KQLDatabase record also
supplies \code{database}.}

\item{query}{A non-empty KQL query.}

\item{database}{KQL database display name. Required for a direct URI or an
Eventhouse record; inferred from a KQLDatabase record.}

\item{parameters}{Named list of values for parameters declared by
\verb{declare query_parameters(...)} in \code{query}.}

\item{request_properties}{Named list of Kusto client request options, such as
\code{servertimeout = "2m"} or \code{notruncation = TRUE}.}

\item{timeout}{Positive request timeout in seconds.}

\item{tenant_id}{Microsoft Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}.}

\item{client_id}{Microsoft Entra application/client ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, with the Azure CLI application ID as fallback.}

\item{access_token}{Optional Kusto bearer token. Supply only one of
\code{access_token} and \code{token_provider}.}

\item{token_provider}{Optional callback returning a Kusto bearer token. It
may accept \code{audience} and \code{force_refresh} arguments.}
}
\value{
A typed tibble for one primary result, a \code{fabric_kql_tables} list for
multiple primary results, or an empty tibble when there is no primary
result.
}
\description{
Executes a read-only Kusto Query Language (KQL) query against a Fabric
Eventhouse query service and converts primary result tables to typed tibbles.
}
\details{
This function uses the Kusto v2 REST query endpoint and requests a token for
\verb{https://api.kusto.windows.net/.default}. The caller needs access to the KQL
database, normally through a Fabric workspace role or KQL database sharing.

Query parameters are sent through Kusto client request properties, never
interpolated into \code{query}. Declare them in KQL with
\verb{declare query_parameters(...)}. Scalar R values are encoded as Kusto
parameter values; vectors and lists are encoded as \code{dynamic(...)} literals.

KQL \code{bool}, \code{datetime}, \code{int}, \code{long}, \code{real}, and \code{timespan} columns become
logical, UTC \code{POSIXct}, integer, \code{bit64::integer64}, double, and \code{difftime}
vectors. \code{dynamic} columns are list-columns, GUIDs and strings are character
vectors, and decimals are doubles. Decimal values outside R's double
precision should be converted to strings in KQL when exact digits are needed.

A query with one primary result table returns a tibble. A query with multiple
primary result tables returns a named list of tibbles with class
\code{fabric_kql_tables}. Auxiliary protocol tables are validated but not returned.
A query with no primary table returns an empty tibble.
Management commands and ingestion endpoints are intentionally not supported.
}
\examples{
\dontrun{
database <- fabric_kql_databases("Telemetry workspace")[1, ]

events <- fabric_kql_query(
  database,
  query = paste(
    "declare query_parameters(selected_type:string);",
    "Events | where EventType == selected_type | take 100"
  ),
  parameters = list(selected_type = "Warning")
)
}
}
