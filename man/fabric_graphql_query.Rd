% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_graphql_query.R
\name{fabric_graphql_query}
\alias{fabric_graphql_query}
\title{Query a Microsoft Fabric API for GraphQL}
\usage{
fabric_graphql_query(
  api,
  query,
  variables = list(),
  operation_name = NULL,
  workspace_id = NULL,
  error_policy = c("return", "warn", "error"),
  timeout = 100,
  idempotent = FALSE,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  audience = .fabric_audience$graphql,
  api_base = .fabric_api_base
)
}
\arguments{
\item{api}{GraphQL HTTPS endpoint, GraphQL API GUID, or one discovered
GraphQLApi record.}

\item{query}{One non-empty GraphQL document.}

\item{variables}{Named list of GraphQL variables.}

\item{operation_name}{Optional operation name for a multi-operation document.}

\item{workspace_id}{Workspace GUID. Required when \code{api} is a GraphQL API
GUID, and otherwise inferred from a discovered record.}

\item{error_policy}{How GraphQL-level errors are handled. \code{"return"}
preserves them in the result, \code{"warn"} also emits a warning, and
\code{"error"} raises a \code{fabric_graphql_error} carrying the result.}

\item{timeout}{Positive request timeout in seconds.}

\item{idempotent}{Logical. Allow shared transient HTTP retries for this POST.}

\item{tenant_id}{Microsoft Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}.}

\item{client_id}{Microsoft Entra application/client ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, with the Azure CLI application ID as fallback.}

\item{token}{Optional \code{AzureAuth::AzureToken}, bearer-token string, or
token-provider function. With \code{NULL}, \code{AzureAuth} reuses a matching cached
token or starts its normal interactive login flow.}

\item{auth_args}{Named list of additional arguments passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}} when no token source is supplied.}

\item{audience}{OAuth audience/scope passed to the credential.}

\item{api_base}{Fabric REST API base URL, used to derive endpoints from IDs.}
}
\value{
A \code{fabric_graphql_result} list with \code{data}, \code{errors}, \code{extensions},
and \code{response} (the complete parsed response).
}
\description{
Executes a GraphQL document against a Fabric GraphQL endpoint and preserves
\code{data}, \code{errors}, and \code{extensions} independently. This is important because
a successful HTTP response can contain both partial data and GraphQL errors.
}
\details{
A direct endpoint has the form
\verb{https://api.fabric.microsoft.com/v1/workspaces/\{workspace-id\}/graphqlapis/\{api-id\}/graphql}.
You can instead pass a GraphQL API GUID with \code{workspace_id}, or one row from
\code{\link[=fabric_graphql_apis]{fabric_graphql_apis()}} or \code{\link[=fabric_item]{fabric_item()}}.

Interactive/delegated authentication requires the Power BI delegated scope
\code{GraphQLApi.Execute.All}, plus \strong{Run Queries and Mutations} permission on
the API. Service principals are also supported by Fabric: request a Fabric
API token with \code{auth_args} or pass one through \code{token}, enable service
principals for Fabric APIs in the tenant, and grant the principal API
Execute access or a suitable workspace role. With SSO connectivity, the
caller also needs the required access to the underlying data source.
Saved-credential APIs use the configured connection instead.

The default \code{audience} is the delegated GraphQL scope. Set it to
\verb{https://api.fabric.microsoft.com/.default} when a custom provider obtains
service-principal tokens. The value is ignored for a static token string.

GraphQL POST requests are not retried by default because a document can
contain mutations. Set \code{idempotent = TRUE} only when the operation is safe
to repeat.
}
\examples{
\dontrun{
api <- fabric_graphql_apis("Analytics workspace")[1, ]

result <- fabric_graphql_query(
  api,
  query = paste(
    "query Products($category: String!) {",
    "  products(filter: {category: {eq: $category}}) {",
    "    items { id name category }",
    "  }",
    "}"
  ),
  variables = list(category = "A"),
  operation_name = "Products"
)
result$data$products$items
result$errors
}
}
